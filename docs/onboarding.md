# Onboarding (Local)

This repo uses **mise** to pin and install CLI tool versions (Terraform, AWS CLI, kubectl, Helm) and uses **AWS IAM Identity Center (SSO)** for authentication.

The goal is: **clone → one command → ready**.

---

## Quick start (one command)

From the repo root:

```bash
./scripts/dev.sh
