# Secret template — DO NOT commit real values.
# Populate before applying:
#
#   kubectl create secret generic demo-app-secrets \
#     --from-literal=pg_password=<password> \
#     --from-literal=sqs_queue_url=<url> \
#     -n demo-app
#
# Or substitute the placeholders and apply this file directly (dev only):
apiVersion: v1
kind: Secret
metadata:
  name: demo-app-secrets
  namespace: demo-app
type: Opaque
stringData:
  pg_password: "REPLACE_ME"
  sqs_queue_url: "REPLACE_ME"
